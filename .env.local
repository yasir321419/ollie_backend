# Local Testing Environment
NODE_ENV=development
PORT=3002

# Database Configuration (Local MySQL Docker)
DATABASE_URL="mysql://root:testpassword@localhost:3306/ollie_test"

# API Configuration  
API_PREFIX=/api/v1

# JWT Security (Test key - generate new for production)
SECRET_KEY=test_local_key_for_development_only_32_chars_minimum_length_required

# Email Configuration (Test values)
MAIL_SERVICE=gmail
MAIL_HOST=smtp.gmail.com
MAIL_USERNAME=test@example.com
MAIL_PASSWORD=testpassword
MAIL_ENCRYPTION=ssl

# CORS Configuration (Allow all for local testing)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Rate Limiting (Relaxed for testing)
RATE_LIMIT_WINDOW=600000
RATE_LIMIT_MAX=10000
AI_RATE_LIMIT_WINDOW=30000
AI_RATE_LIMIT_MAX=100

# Debug Settings (Enable for testing)
AI_DEBUG_LOGGING=true
AI_FULL_LOGGING=true